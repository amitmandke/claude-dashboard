node_modules/
*.log
.DS_Store

# Staging area for not-yet-built proposals; graduates into DESIGN.md when built.
# Intentionally untracked — never commit it (see CLAUDE.md).
docs/proposals.md
# Local design mocks (frontend-design skill output) — not part of the shipped app.
docs/*-mock.html
